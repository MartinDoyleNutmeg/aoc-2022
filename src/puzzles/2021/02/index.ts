import { logAnswer } from '../../../utils';

const runOne = () => {
  // NOTE: Completed on different computer!
  logAnswer(1, 1_484_118);
};

const runTwo = () => {
  // NOTE: Completed on different computer!
  logAnswer(2, 1_463_827_010);
};

export default async function () {
  runOne();
  runTwo();
}
